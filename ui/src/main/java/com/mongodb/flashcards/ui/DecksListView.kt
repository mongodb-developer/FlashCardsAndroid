package com.mongodb.flashcards.ui

import androidx.compose.runtime.Composable
import androidx.navigation.NavController

@Composable
fun DecksListView(navController: NavController) {
    AppToolbar(title = "Decks") {
    }
}
